#include <iostream>
#include "ICypher.h"
using namespace std;

int main()
{




	/*
	string st="Hello";
	string key = "jfoefefm";
	cout << st;
	unsigned int k = 0;
	for (unsigned int i = 0; i < st.length(); ++i, ++k)
	{
		if(k==key.length())
		{
			k = 0;
		}
		st[i] ^= key[k];
	}
	
	
	cout << st << " end\n";
	k = 0;
	for (unsigned int i = 0; i < st.length(); ++i, ++k)
	{
		if (k == key.length())
		{
			k = 0;
		}
		st[i] ^= key[k];
	}
	cout << st;
	*/

	cin.get();
}